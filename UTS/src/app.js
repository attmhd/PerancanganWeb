// const navLink = document.querySelectorAll('.navlink')
// const sectionId = document.querySelectorAll('.section')

// let currentSect = 'about_me'
// window.addEventListener('scroll', () => {
//     sectionId.forEach(sectionId => {
//         if (window.scrollY >= sectionId.offsetTop) {
//             currentSect = sectionId.id
//         }
//     })

//     navLink.forEach(navLink => {
//         if (navLink.href.includes(currentSect)) {
//             navLink.classList.add('aktif')
//         }
//     })
// })